---
# Inventory for Vagrant ansible_local provisioner
# Ansible runs inside VM, targeting localhost
all:
  hosts:
    localhost:
      ansible_connection: local
      ansible_python_interpreter: /usr/bin/python3
